export interface MetricsModuleOptions {
  /**
   * Collect default metrics
   */
  collectDefaultMetrics: boolean;
  /**
   * Collect metrics every N milliseconds
   */
  collectMetricsEveryNMilliseconds: number;
}
